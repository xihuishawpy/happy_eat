# Public MVP uses a family access code

The MVP may be deployed on the public internet, but it will not include personal accounts or family member permissions. Real family-space data is protected by a shared family access code so the app remains usable outside the home without committing to a full authentication and invitation model yet.
